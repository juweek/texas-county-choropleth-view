import React from 'react';
import { BrowserRouter } from 'react-router-dom';

const App: React.FC = () => {
  return (
    <BrowserRouter basename="/texas-county-choropleth-view">
      {/* Your app content */}
    </BrowserRouter>
  );
};

export default App; 